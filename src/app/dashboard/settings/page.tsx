'use client'
// src/app/dashboard/settings/page.tsx
// User account settings — placeholder for now.

export default function SettingsPage() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--sp-5)', maxWidth: 600 }}>
      <div className="page-header">
        <div className="page-title">Settings</div>
      </div>
      <div style={{ border: '1px solid var(--border)', borderRadius: 'var(--r-lg)', padding: '16px', color: 'var(--text-3)', fontSize: 'var(--fs-sm)' }}>
        Account settings coming soon.
      </div>
    </div>
  )
}
